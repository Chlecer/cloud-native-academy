apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  labels:
    app: webapp
spec:
  type: NodePort
  selector:
    app: webapp    # Seleciona pods independente da versão
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30100
    name: http